# On Streamlit Community Cloud: paste this under "Advanced settings -> Secrets".
# Locally: copy to ".streamlit/secrets.toml" (already gitignored) OR just use a .env file.
GEMINI_API_KEY = "your_key_here"
